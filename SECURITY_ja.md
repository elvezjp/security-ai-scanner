# セキュリティポリシー

[English](./SECURITY.md) | [日本語](./SECURITY_ja.md)

## サポート対象バージョン

最新バージョンをサポートします。

| バージョン | サポート           |
| ------- | ------------------ |
| 0.2.x   | :white_check_mark: |
| < 0.2   | :x:                |

## 脆弱性の報告

security-ai-scanner 自体の脆弱性を発見した場合は、責任ある開示の
プラクティスに従ってください。

### 報告方法

重大度にかかわらず、**GitHub Private Security Advisory** で報告してください。

1. 脆弱性については公開の GitHub Issue を**作成しないでください**
2. Private Security Advisory をこちらから作成してください:
   https://github.com/elvezjp/security-ai-scanner/security/advisories/new

脆弱性では**ない**セキュリティ関連の質問（ベストプラクティス、設定など）は、
[質問](#質問) セクションを参照してください。

### 記載してほしい内容

- 脆弱性の説明
- 再現手順
- 想定される影響と重大度
- 修正・緩和策の提案
- 連絡先（任意）

### 対応スケジュール

- **初回応答**: 48時間以内
- **状況更新**: 7日以内
- **解決**: 重大度に応じて
  - 緊急: 14日以内
  - 高: 30日以内
  - 中: 60日以内
  - 低: 次回リリースサイクル

## スキャン対象リポジトリで見つかった脆弱性について

security-ai-scanner が**他のリポジトリ**で発見した脆弱性は、その
リポジトリの所有者に帰属します。そのプロジェクトのセキュリティポリシー
または協調的開示プロセスに従い、受け取る権限のある人にのみ共有して
ください。第三者のコードのスキャン結果を本プロジェクトの Issue に
投稿しないでください。

## スキャンを安全に実行するために

- **自身が所有するか、明示的に評価の許可を得ているコードのみをスキャンしてください**
- 解析のためにリポジトリの内容が設定された AI エンジンに送信されます。
  スキャンの前に、そのエンジンへのコード送信が組織のルールや適用される
  契約で許可されていることを確認してください
- スキャンエージェントは読み取り専用ツール（Read / Glob / Grep）で動作し、
  シェル実行・ファイル書き込み・ネットワークツールはエンジン設定で禁止されています
- リポジトリのファイル・コメント・ユーザー指定の `--context` は信頼できない
  入力として扱ってください。スキャンプロンプトはその前提でエージェントに
  指示していますが、プロンプトインジェクションを完全に排除することは
  できません。所見に基づいて行動する前にレビューしてください
- スキャン出力（`findings.json`・`findings.sarif`・`report.md`）には
  脆弱性の詳細とコード片が含まれます。ソースコードと同等の管理で保管し、
  公開の Issue やプルリクエストに含めないでください
- 所見には誤検出・見逃しが含まれ得ます。レポートは人によるレビューへの
  専門的な入力であり、セキュリティの保証ではありません

## 依存関係のセキュリティ監視

依存関係は Dependabot で監視しています。設定は[`.github/dependabot.yml`](.github/dependabot.yml) にあり、`uv` と
`github-actions` について月次で更新 PR を出します。GitHub Actions は
可変タグではなくコミット SHA で固定しているため、更新は必ずレビュー
可能な PR を経由し、タグの指し先が変わって黙って別のコードを実行する
ことはありません。

Dependabot アラートは以下の方針でトリアージします：

| アラート種別 | 対応 |
|------|------|
| Malware | 発生場所を問わず必ず即時修正する |
| Vulnerable（最新バージョン） | 依存更新 PR で修正する。依存パッケージのバージョン更新のみで解消する場合、本体のバージョンは変更しない |
| Vulnerable（アーカイブ・旧バージョン） | 対象コードパスが保守対象バージョンに含まれないことを確認したうえで Dismiss する |

## セキュリティのベストプラクティス

security-ai-scanner を使用する際の推奨事項：

1. **最新版を使う**: 常に最新バージョンを使用してください
2. **スキャン範囲を絞る**: 自身が所有するか、評価の許可を得ているコードのみをスキャンしてください
3. **エンジンの送信先を確認する**: 機密性の高いコードをスキャンする前に、`--base-url` の接続先が意図通りか確認してください
4. **所見を鵜呑みにしない**: スキャン結果は人によるレビューへの専門的な入力として扱い、保証とはみなさないでください
5. **スキャン出力を保護する**: `findings.json`・`findings.sarif`・`report.md` はソースコードと同等の管理で保管し、公開の Issue や PR に含めないでください
6. **依存関係を監視する**: `claude-agent-sdk` などの依存関係を最新に保ってください

## 質問

脆弱性ではないセキュリティ関連の質問は info@elvez.co.jp までご連絡ください。
